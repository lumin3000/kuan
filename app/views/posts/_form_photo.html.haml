%li
  %a{ :href => (form_photo.nil? ? "{image_a}" : form_photo.image.url_for(:original)), :rel => 'lightbox' }
    %img{ :src => (form_photo.nil? ? "{image}" : form_photo.image.url_for(:small)), :alt => '', :style => 'width:30px;height:30px' }
  %span
    = text_field_tag "photos[][desc]", form_photo.nil? ? "{desc}" : form_photo.desc
    = hidden_field_tag "photos[][image]", (form_photo.nil? ? "{id}" : form_photo.image.id)
    - if not form_photo.nil?
      = hidden_field_tag "photos[][id]", form_photo.id
