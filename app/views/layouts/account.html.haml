- @header = @footer = "common"
- @nav_now = "account"
- content_for :content, do
  = yield
  = render :partial => 'shared/footer'

- css :user

- content_for :site_layout, "site_middle_size account"

= render :file => 'layouts/application'
