- if message.done?
  %div 已通过
- elsif message.ignored?
  %div 已忽略
- else
  %div
    %a{href: "/messages/#{message.id}/doing", data: {method: "post", remote: "true"}} 通过
  %div
    %a{href: "/messages/#{message.id}/ignore", data: {method: "post", remote: "true"}} 忽略
